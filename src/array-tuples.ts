// array
const members: string[] = ['Arif', 'Binu', 'Ariba'];
const ages: number[] = [22, 21, 1];
const personInfo: (string | number | boolean)[] = ['Arif', 22, true];

// tuples
// tuple: Tuple is like array. But tuple data type declare with array index. If we declare 2 index data type into tuple so we only use 2 element into array. I mean tuple data type declartion and tuple element must be equal

const personTuple: [string, number, boolean] = ['Arif', 23, true];
