// // null
// const searchAnything = (term: string | null): void => {
//   if (term === null) {
//     console.log(`Nothing to search for ${term}`);
//   } else {
//     console.log(`Searching anything for ${term}`);
//   }
// };

// searchAnything(null);
// searchAnything('ariba');

// // unknown
// const guessSomething = (guess: unknown): void => {
//   console.log(guess);
// };

// guessSomething('arif');
// guessSomething(12);
// guessSomething(null);
// guessSomething(false);
// guessSomething(undefined);
